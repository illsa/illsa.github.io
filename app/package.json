{
  "name": "jekyll-editorial",
  "version": "11.11.11",
  "description": "",
  "main": "index.js",
  "scripts": {
    "app:dev": "bundle install && bundle update && bundle exec jekyll serve --incremental --verbose --port 4050",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeffrey Desir",
  "license": "MIT"
}
